# GitHub Actions workflow to automatically publish to GitHub Packages.
# Note: JitPack builds are triggered automatically from git tags.
# To publish to JitPack, create a release/tag and JitPack will build on-demand.
name: "Publish to GitHub Packages"
on:
  push:
    branches: [ "main" ]

env:
  JAVA_VERSION: 17

jobs:
  publish:
    name: "Publish"
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: "Checkout repository"
        uses: actions/checkout@v3

      - name: "Set up Java ${{ env.JAVA_VERSION }}"
        uses: actions/setup-java@v3
        with:
          java-version: "${{ env.JAVA_VERSION }}"
          distribution: "adopt"

      - name: "Grant execute permission for gradlew"
        run: chmod +x gradlew

      - name: "Gradle publish"
        uses: gradle/gradle-build-action@v2
        with:
          arguments: "clean build publishAllPublicationsToGitHubPackagesRepository"
        env:
          GITHUB_ACTOR: ${{ github.actor }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
